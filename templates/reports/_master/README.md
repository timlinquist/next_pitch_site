# Mechanics evaluation master

Do not edit athlete reports here. Copy via:

```bash
python3 tools/mechanics-report/new_report.py create --athlete "Name" --stills /path/to/stills
```

- `index.source.html` uses `__ATHLETE_NAME__` and `__ATHLETE_SLUG__`.
- `serve.py` is copied into each athlete folder.
